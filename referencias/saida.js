/**
 * A saída de texto no JavaScript pode ser feita com `console.log()`.
 * Isto serve para ferramentas de linha de comando e para exibir texto no
 * console do navegador quando estamos desenvolvendo para web. Em aplicações
 * web ou mobile, usaremos a interface da framework em vez disto.
 */
console.log('Olá, mundo!');
